import 'package:flutter/material.dart';

class QuestionsScreen extends StatelessWidget {
  const QuestionsScreen({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text("quiz or some shit"));
  }
}
